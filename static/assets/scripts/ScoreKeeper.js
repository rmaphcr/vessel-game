class ScoreKeeper
{
	//this class tallies up the score at the end of each level
	constructor(scene, weights)
	{
		this.scene = scene
		this.weights = weights
	}
	
	tallyScore()
	{
			//counts up the score
			//returns it
			//so the level class can add it to the scene's timeRemaining
	}
}
